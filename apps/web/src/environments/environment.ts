// Default (development) environment. For local `ng serve`. Override
// apiBaseUrl here if you'd rather hit the deployed Railway API instead
// of a local FastAPI process.
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8000',
};
