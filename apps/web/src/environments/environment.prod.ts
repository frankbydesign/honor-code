// Production environment. Used by Vercel builds via the
// fileReplacements entry in angular.json.
export const environment = {
  production: true,
  apiBaseUrl: 'https://honor-code-production.up.railway.app',
};
